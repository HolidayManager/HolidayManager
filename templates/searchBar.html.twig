{% extends 'Dashboard/navbar.html.twig' %}
{% block body %}
    {% if is_granted('ROLE_ADMIN') %}

        <div class="container">

            {{form(searchBar)}}
        </div>
    {% endif %}
{% endblock %}

